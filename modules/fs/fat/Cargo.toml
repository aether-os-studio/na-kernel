[package]
name = "fat"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
na-std = { path = "../../na-std", features = ["module-runtime"] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = "debuginfo"
